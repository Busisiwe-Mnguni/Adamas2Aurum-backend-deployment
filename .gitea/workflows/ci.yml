name: CI Pipeline

on:
  push:
    branches: [main, dev]
  pull_request:
    branches: [main, dev]

jobs:
  placeholder:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout code
        uses: actions/checkout@v4
      - name: Placeholder — replace once project is scaffolded
        run: echo "CI is wired up. Real lint/test steps go here once frontend/backend exist."